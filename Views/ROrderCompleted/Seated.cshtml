@model IEnumerable<MVCRestaurant27Tem2022.Models.ROrderCompleted>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h1> Table @ViewBag.Title</h1>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">

            <div class="col-md-10">
                @*@Html.EditorFor(model => model.tstatus, new { htmlAttributes = new { @class = "form-control" } })*@
                @*@Html.ValidationMessageFor(model => model.tstatus, "", new { @class = "text-danger" })*@
            </div>
        </div>
        @ViewBag.BillSum
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <h2 style="color:darkred">@ViewBag.Error</h2><br />
                <input type="submit" name="submit" value="Checkout" class="btn btn-default" />
                <input type="submit" name="submit" value="order" class="btn btn-default" />
            </div>
        </div>
    </div>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FoodDrink.FDname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_bill)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_waiter)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.odatetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FoodDrink.price)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        if (item.id_bill == Convert.ToInt64(Session["billId"]))
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.FoodDrink.FDname)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.id_bill)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.id_waiter)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.odatetime)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.FoodDrink.price)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.id_order }) |
        @Html.ActionLink("Details", "Details", new { id = item.id_order }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.id_order })
    </td>
</tr>
        }
    }
</table>
